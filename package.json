{
  "name": "mainspring",
  "version": "0.0.0",
  "private": true,
  "description": "A private, offline-first FIRE planner. Wind it up — it drives everything.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "tauri": "pnpm --filter desktop tauri",
    "dev": "pnpm --filter desktop tauri dev",
    "build": "pnpm --filter desktop tauri build",
    "test": "pnpm -r --if-present test"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@11.8.0"
}
