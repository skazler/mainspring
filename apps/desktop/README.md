# MAINSPRING — desktop app

The Tauri 2 + Svelte 5 shell for MAINSPRING. See the [root README](../../README.md)
for the architecture, stack, and full docs.

```bash
pnpm tauri dev   # from the repo root — launches the app with the local PGlite vault
```
